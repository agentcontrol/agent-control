/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Rule specification. See GET /rules for available rules and schemas.
 *
 * @remarks
 *
 * Rule reference formats:
 * - Built-in: "regex", "list", "json", "sql"
 * - External: "galileo.luna" (requires agent-control-rules[galileo])
 * - Agent-scoped: "my-agent:my-rule" (validated in endpoint, not here)
 */
export type RuleSpec = {
  /**
   * Rule-specific configuration
   */
  config: { [k: string]: any };
  /**
   * Rule name or agent-scoped reference (agent:rule)
   */
  name: string;
};

/** @internal */
export const RuleSpec$inboundSchema: z.ZodMiniType<RuleSpec, unknown> = z
  .object({
    config: z.record(z.string(), z.any()),
    name: types.string(),
  });
/** @internal */
export type RuleSpec$Outbound = {
  config: { [k: string]: any };
  name: string;
};

/** @internal */
export const RuleSpec$outboundSchema: z.ZodMiniType<
  RuleSpec$Outbound,
  RuleSpec
> = z.object({
  config: z.record(z.string(), z.any()),
  name: z.string(),
});

export function ruleSpecToJSON(ruleSpec: RuleSpec): string {
  return JSON.stringify(RuleSpec$outboundSchema.parse(ruleSpec));
}
export function ruleSpecFromJSON(
  jsonString: string,
): SafeParseResult<RuleSpec, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RuleSpec$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RuleSpec' from JSON`,
  );
}
