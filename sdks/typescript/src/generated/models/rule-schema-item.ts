/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Rule schema summary for list response.
 */
export type RuleSchemaItem = {
  configSchema: { [k: string]: any };
  description: string | null;
  name: string;
};

/** @internal */
export const RuleSchemaItem$inboundSchema: z.ZodMiniType<
  RuleSchemaItem,
  unknown
> = z.pipe(
  z.object({
    config_schema: z.record(z.string(), z.any()),
    description: types.nullable(types.string()),
    name: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "config_schema": "configSchema",
    });
  }),
);

export function ruleSchemaItemFromJSON(
  jsonString: string,
): SafeParseResult<RuleSchemaItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RuleSchemaItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RuleSchemaItem' from JSON`,
  );
}
