/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Evaluator schema summary for list response.
 */
export type EvaluatorSchemaItem = {
  name: string;
  description: string | null;
  configSchema: { [k: string]: any };
};

/** @internal */
export const EvaluatorSchemaItem$inboundSchema: z.ZodMiniType<
  EvaluatorSchemaItem,
  unknown
> = z.pipe(
  z.object({
    name: types.string(),
    description: types.nullable(types.string()),
    config_schema: z.record(z.string(), z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "config_schema": "configSchema",
    });
  }),
);

export function evaluatorSchemaItemFromJSON(
  jsonString: string,
): SafeParseResult<EvaluatorSchemaItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EvaluatorSchemaItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EvaluatorSchemaItem' from JSON`,
  );
}
