/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Information about a registered rule.
 */
export type RuleInfo = {
  /**
   * JSON Schema for config
   */
  configSchema: { [k: string]: any };
  /**
   * Rule description
   */
  description: string;
  /**
   * Rule name
   */
  name: string;
  /**
   * Whether rule requires API key
   */
  requiresApiKey: boolean;
  /**
   * Default timeout in milliseconds
   */
  timeoutMs: number;
  /**
   * Rule version
   */
  version: string;
};

/** @internal */
export const RuleInfo$inboundSchema: z.ZodMiniType<RuleInfo, unknown> = z.pipe(
  z.object({
    config_schema: z.record(z.string(), z.any()),
    description: types.string(),
    name: types.string(),
    requires_api_key: types.boolean(),
    timeout_ms: types.number(),
    version: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "config_schema": "configSchema",
      "requires_api_key": "requiresApiKey",
      "timeout_ms": "timeoutMs",
    });
  }),
);

export function ruleInfoFromJSON(
  jsonString: string,
): SafeParseResult<RuleInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RuleInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RuleInfo' from JSON`,
  );
}
