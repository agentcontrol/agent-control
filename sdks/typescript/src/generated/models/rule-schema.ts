/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Schema for a custom rule registered with an agent.
 *
 * @remarks
 *
 * Custom rules are Rule classes deployed with the engine.
 * This schema is registered via initAgent for validation and UI purposes.
 */
export type RuleSchema = {
  /**
   * JSON Schema for rule config validation
   */
  configSchema?: { [k: string]: any } | undefined;
  /**
   * Optional description
   */
  description?: string | null | undefined;
  /**
   * Unique rule name
   */
  name: string;
};

/** @internal */
export const RuleSchema$inboundSchema: z.ZodMiniType<
  RuleSchema,
  unknown
> = z.pipe(
  z.object({
    config_schema: types.optional(z.record(z.string(), z.any())),
    description: z.optional(z.nullable(types.string())),
    name: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "config_schema": "configSchema",
    });
  }),
);
/** @internal */
export type RuleSchema$Outbound = {
  config_schema?: { [k: string]: any } | undefined;
  description?: string | null | undefined;
  name: string;
};

/** @internal */
export const RuleSchema$outboundSchema: z.ZodMiniType<
  RuleSchema$Outbound,
  RuleSchema
> = z.pipe(
  z.object({
    configSchema: z.optional(z.record(z.string(), z.any())),
    description: z.optional(z.nullable(z.string())),
    name: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      configSchema: "config_schema",
    });
  }),
);

export function ruleSchemaToJSON(
  ruleSchema: RuleSchema,
): string {
  return JSON.stringify(RuleSchema$outboundSchema.parse(ruleSchema));
}
export function ruleSchemaFromJSON(
  jsonString: string,
): SafeParseResult<RuleSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RuleSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RuleSchema' from JSON`,
  );
}
