"""Shipped auth providers.

A provider implements the ``RequestAuthorizer`` protocol defined in
``auth.core``. Providers map Agent Control's generic :class:`Operation`
vocabulary onto a concrete authentication/authorization system.
"""
